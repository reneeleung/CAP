# Mapping between shorthand names (used in code) and full names (used in annotation_config.json)
ATTRIBUTE_MAPPING = {
    'time': 'time_nature',
    'criteria': 'SLEDAI_criteria',
    'special': 'special_entity',
    'intention': 'nature_of_intention_to_treat',
}

ATTRIBUTE_LIST = list(ATTRIBUTE_MAPPING.keys())
